apiVersion: v1
kind: Service
metadata:
  name: office-locator-svc
  namespace: php-poc
spec:
  type: LoadBalancer
  selector:
    app: office-locator
  ports:
    - port: 80
      targetPort: 80
